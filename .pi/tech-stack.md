---
purpose: Detected tech stack, commands, constraints, integrations, environments, and unknowns for AI context injection
updated: 2026-08-13
---

# Tech Stack

This file records the detected tech stack; read it on demand for project
context. Distinguish project dependencies from host tools: a host tool becomes a
stack entry only when a manifest, script, workflow, or explicit user decision
uses it. Every value carries version or command evidence.

## Framework & Language

- **Framework:** None — plain TypeScript stdio adapter. Evidence: `package.json` (no framework deps), `src/index.ts`.
- **Language:** TypeScript (ESM, `type: module`). Evidence: `package.json`, `tsconfig.json`.
- **Runtime:** Node.js >= 20 (`engines.node`); workspace host v26.7.0. Evidence: `package.json`, `node --version`.
- **Project manifest:** `package.json` (`pi-acp@0.0.33`). Evidence: path.

## Project Dependencies vs Host Tools

- **Project dependencies:** `@agentclientprotocol/sdk ^0.26.0` (ACP protocol), `typebox ^1.3.8` (JSON Schema → TypeBox in the pi extension), `zod ^3.25.0`. Evidence: `package.json`.
- **Host tools (installed, not project dependencies):**
  | Tool | Version | Evidence | Used by the project? |
  | --- | --- | --- | --- |
  | pi coding-agent | v0.84.1 (devDependency for types + runtime target) | `package.json` devDeps, `PI_ACP_PI_COMMAND` | yes — spawned per session |
  | Node.js | v26.7.0 | `node --version` | yes — runtime |
  | npm | 11.19.0 | `npm --version` | yes — scripts |
  | gh CLI | 2.96.0, authenticated as `ryan-brosas` | `gh --version`, `gh auth status` | yes — release/push workflow (optional) |
  | JetBrains IntelliJ | 2026.2 (ACP host + private MCP server) | `~/.jetbrains/acp.json`, live probes | yes — primary host |

## Styling & UI

- None — stdio service; no UI surface. Evidence: `src/index.ts` (ndJsonStream).

## Data & State

- **Database:** None.
- **State:** `~/.pi/pi-acp/session-map.json` (ACP → pi session mapping); pi session files under `~/.pi/agent/sessions/`. Evidence: `src/acp/session-store.ts`, `src/acp/pi-sessions.ts`.
- **API Style:** JSON-RPC 2.0 (ACP) over stdio; newline-delimited JSON to pi `--mode rpc`; HTTP+SSE to IntelliJ's private MCP server. Evidence: `src/index.ts`, `src/pi-rpc/process.ts`, `src/acp/mcp-sse.ts` (fork `main`).

## Commands

Verify each command before recording it. A command that does not exist is
recorded as "none" with the probe result.

| Command                  | Status                       | Purpose                       | Verified                                                                                |
| ------------------------ | ---------------------------- | ----------------------------- | --------------------------------------------------------------------------------------- |
| `npm install`            | not rerun (existing install) | install deps                  | npm 11.19.0 available; avoided lockfile/node_modules mutation during init               |
| `npm run dev`            | script exists; not executed  | run from src via tsx          | `package.json` inspection (`tsx src/index.ts`)                                          |
| `npm test`               | works                        | unit + component tests        | 2026-08-13, exit 0, 118 tests (workspace checkout; 139 at fork `main`)                  |
| `npm run lint`           | works                        | eslint                        | 2026-08-13, exit 0 after declaring browser-context globals in the imported skill helper |
| `npm run typecheck`      | works                        | tsc --noEmit                  | 2026-08-13, exit 0                                                                      |
| `npm run build`          | works                        | tsup → `dist/`                | 2026-08-13, exit 0                                                                      |
| `npm run format`         | script exists; not executed  | prettier write-mode formatter | `package.json` inspection; not run because it mutates the whole tree                    |
| `node scripts/check.mjs` | works                        | template-layer canonical gate | 2026-08-13, exit 0, 111 oks                                                             |

## CI

- **Workflows:** `check.yml` (canonical template gate), `github-release.yml` (release), `npm-publish.yml` (npm publish). Evidence: `.github/workflows/`.
- **Local reproduction:** `node scripts/check.mjs` mirrors `check.yml`. Adapter gates (`npm test`, lint, typecheck) are not wired into CI yet — gap.

## Generated Files

- `dist/` is generated by `tsup` (`npm run build`). Never edit by hand.
- `.pi/fabric/`, `.pi/MEMORY.md`, `.pi/implementation-notes.md`, `.veda/`, `.eslintcache` are runtime/tool state; gitignored.

## Testing

- **Unit Tests:** node:test via tsx — `test/unit/*` (18 files). Evidence: `package.json` test script.
- **Component Tests:** node:test — `test/component/*` (session lifecycle, queue, slash commands). Evidence: `find test`.
- **E2E:** `scripts/smoke-*.mjs` headless ACP probes; live IntelliJ probes (manual).
- **Coverage Target:** none configured.
- **Coverage gaps:** IntelliJ-specific behavior is covered by manual live probes only; no ACP-host integration suite.

## Active Integrations

- **JetBrains IntelliJ ACP host** — supplies per-chat MCP descriptors (`IJ_MCP_SERVER_PORT`) and hosts the private IDE MCP server; primary path is SSE-first with stdio fallback. Evidence: `src/acp/mcp-bridge.ts`, `~/.jetbrains/acp.json`.
- **pi coding agent** — `pi --mode rpc` subprocess per ACP session. Evidence: `src/pi-rpc/process.ts`.
- **pi extension host** — `ide_<server>_<tool>` tools over authenticated per-session IPC. Evidence: `src/pi-extension/acp-mcp-bridge.ts`, `src/acp/mcp-ipc.ts`.

## Environments

- Local development only. `dist/index.js` is the deployable artifact; IntelliJ wires it via `~/.jetbrains/acp.json` `agent_servers`.

## Key Constraints

- Node >= 20 (`package.json` `engines.node`); pi v0.80.4+ (`README.md` prerequisites).
- JetBrains IntelliJ is the primary actively developed ACP host (explicit user direction, 2026-08-13).
- No ACP client-side filesystem or terminal delegation; pi operates locally (`README.md` Limitations).
- Workspace checkout is pre-SSE (`e17ed85`); sync to fork `main` (`f6e5ab2`) is roadmap Phase 2.

## Unknowns

- `[NEEDS CLARIFICATION: release/version policy for the JetBrains-focused fork is not documented]`
- `[NEEDS CLARIFICATION: IntelliJ's private MCP/SSE API has no stability guarantee exposed in this repository]`

## Context Budget Guidelines

**Quality Degradation Rule:** target about 50% context per plan execution.

| Task Complexity                | Max Tasks/Plan | Typical Context Usage |
| ------------------------------ | -------------- | --------------------- |
| Simple                         | 3              | ~30-45%               |
| Complex/cross-transport        | 2              | ~40-50%               |
| Very complex/session lifecycle | 1-2            | ~30-50%               |

Split when work spans multiple subsystems, requires discovery plus implementation, or modifies more than five files per task.

## Verification Commands

**Run before claiming complete:**

```bash
node scripts/check.mjs
npm test
npm run lint
npm run typecheck
npm run build
```

All recorded gates pass in this checkout after adapting the imported browser helper for ESLint. Fork `main` remains the code-claim source for SSE behavior until Phase 2 consolidation.

---

_Update this file when the stack, commands, or constraints change._
